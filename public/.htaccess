RewriteEngine On
RewriteBase /worldcup/

# Route all requests through the front controller when files/directories do not exist.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]

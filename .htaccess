RewriteEngine On
RewriteBase /wc-game/

# Route all non-file/directory requests through the public front controller
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ public/index.php [QSA,L]
